# BADDLE — Neon Dark (Red Accent) v4

Единый набор данных (один большой “пак”). Без переключателя packs.

## Запуск
Открой `index.html`.

## Где заполнять участников и вопросы
Файл: `app.js`

### ROSTER
Заполняй:
- `name`, `aliases`, `avatar`
- `lolRole` (ADC/Mid/Jungle/Support/Top)
- `voice` (Always/Often/Rare)
- `timezone` (например UTC+2)
- `joined` (<1m/<3m/<6m/>6m)
- `emojis` (до 5)

### QUESTIONS
- `classic[]` и `emoji[]` содержат `targetId` (ссылка на ROSTER)
- `profile[]` — тоже `targetId` + `image` (если нужно отдельное изображение)
- `next[]` — клипы/кадры с вариантами

## Кнопки
- 🆕 Новая игра — сбрасывает “мешок” (no-repeat) и начинает заново
- 🎲 Случайный — случайный вопрос в текущем режиме
- ↻ Сброс — сброс попыток на текущем вопросе


## Автодополнение (как в LoLdle)
В Classic/Emoji/Profile при вводе ника появляется выпадающий список с участниками (можно выбрать мышкой или стрелками ↑↓, Tab/Enter).


## Classic параметры
Classic теперь сравнивает:
- LoL Role (или '-')
- Main Champ (или '-')
- Rank (жёлтый если ±1 рядом)
- Server Role (mommy / министр додепа / сучка / сладенькие / полусладенькие / без роли)
